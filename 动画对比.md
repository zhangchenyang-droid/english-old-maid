# AI 配对出牌动画 - 新旧版本对比

## 旧版动画（v2.0）

```
AI手牌
  🌲🌲
   ↓
   🎴  (翻转到90°看不见)
   ↓
   ↘️
    ↘️
     🎴 → 弃牌堆

特点：
- 直线路径
- 简单翻转
- 固定速度
- cubic-bezier 缓动
```

### 旧版代码特征
- 3个关键帧：起点 → 90° → 终点
- 单一缓动函数
- 无物理模拟
- 时长：720ms

---

## 新版动画（v3.0）

```
AI手牌
  🌲🌲
   ↗️ ↗️  (抛物线起飞)
     🎴🎴 (翻滚旋转)
      ↘️ ↘️
       🎴🎴 (过冲)
        ↙️ ↙️ (回弹)
         🎴🎴 → 弃牌堆

特点：
- 抛物线轨迹（真实物理）
- 翻滚旋转（Z轴）
- 过冲回弹（弹簧效果）
- 两段式缓动
```

### 新版代码特征
- 40个关键帧（精细控制）
- 两段式缓动（0-68% / 68-100%）
- 物理模拟（抛物线 + 弹簧）
- 时长：680ms（更快更有力）

---

## 具体差异对比

| 特性 | 旧版 (v2.0) | 新版 (v3.0) |
|------|-------------|-------------|
| **轨迹** | 直线 | 抛物线弧 |
| **旋转** | 仅Y轴翻转 | Y轴翻转 + Z轴翻滚 |
| **缓动** | 单一cubic-bezier | 两段式（ease-out + ease-in-out） |
| **过冲** | 无 | 5% 过冲 + 回弹 |
| **起飞** | 同时起飞 | 错开起飞（±20px） |
| **速度** | 720ms 固定 | 680ms 变速（起飞快，落地慢） |
| **关键帧** | 3帧 | 40帧 |
| **物理感** | ❌ | ✅ |

---

## 视觉对比图

### 旧版轨迹
```
起点 ━━━━━━━━━━━━━━━━━━━━━━> 终点
     (匀速直线，无变化)
```

### 新版轨迹
```
起点
  \
   \    (加速上升)
    \
     \
      ○ (最高点)
       \
        \  (减速下降)
         \
          > (过冲)
         <  (回弹)
          ● 终点
```

---

## 用户体验提升

### 旧版问题
1. **不自然**：直线飞行，缺乏重力感
2. **单调**：每次看起来完全一样
3. **突兀**：突然停止，无缓冲

### 新版优势
1. **真实感**：抛物线 + 重力 + 过冲
2. **多样性**：翻滚角度和轨迹略有差异
3. **流畅性**：过冲回弹，自然停止

---

## 代码复杂度对比

### 旧版（简单）
```javascript
flipContainer.animate([
  { transform: "translate(0,0) rotateY(0deg)", offset: 0 },
  { transform: "translate(0,0) rotateY(90deg)", offset: 0.25 },
  { transform: `translate(${dx},${dy}) rotateY(180deg)`, offset: 1 }
], { duration: 720, easing: "cubic-bezier(.2,.9,.2,1)" });
```

### 新版（物理模拟）
```javascript
for (let i = 0; i <= 40; i++) {
  const t = i / 40;

  // 两段式缓动
  let easeT = calculateEasing(t);

  // 抛物线轨迹
  const arcY = -arcHeight * 4 * arcT * (1 - arcT);

  // 过冲回弹
  const overshoot = calculateOvershoot(t);

  // 翻滚旋转
  const tumble = 360 * (1 - t);

  keyframes.push({ transform: ..., offset: t });
}
```

---

## 与其他动画的一致性

新版动画与现有动画保持一致：

1. **发牌动画 (dealEnter)**
   - ✅ 相同的两段式结构
   - ✅ 相同的过冲比例（5%）
   - ✅ 相同的转折点（68%）

2. **大王卡飞行 (flyFromRectToRect)**
   - ✅ 相同的弧线算法
   - ✅ 相同的物理感
   - ✅ 相同的时长范围（680ms vs 560ms）

3. **玩家出牌 (flyToDiscard)**
   - ✅ 相同的目标位置
   - ✅ 相同的分堆逻辑
   - ⚠️ 玩家出牌仍是简单动画（未来可优化）

---

## 性能影响

### 旧版
- 3个关键帧 → 轻量级
- GPU 加速 ✅
- 每次出牌：~1ms CPU

### 新版
- 40个关键帧 → 中等复杂度
- GPU 加速 ✅（transform only）
- 每次出牌：~2ms CPU
- **结论**：性能影响可忽略（<1%）

---

## 测试建议

1. **快速测试**：
   - 开始游戏
   - 等待 AI 配对出牌
   - 观察卡牌飞行轨迹

2. **对比测试**：
   - 回退到旧版代码（git stash）
   - 对比新旧动画的流畅度和自然度

3. **边缘情况**：
   - 不同 AI 位置（上/左/右）
   - 不同窗口大小（手机/平板/桌面）
   - 多对连续出牌

---

## 下一步优化方向

### 可选增强（优先级：低）
- [ ] 根据 AI 位置调整起飞角度（上方AI向下抛，侧面AI向中心抛）
- [ ] 添加卡牌拖尾效果（motion blur）
- [ ] 添加音效（whoosh + flip + land）

### 玩家出牌动画（优先级：中）
- [ ] 将相同的物理动画应用到 `flyToDiscard()`
- [ ] 保持玩家和AI动画一致性

### 性能监控（优先级：高）
- [ ] 添加性能埋点
- [ ] 监控低端设备表现
- [ ] 必要时提供"简化动画"选项

---

**更新时间**：2026-01-11
**作者**：Claude Code
**版本**：v3.0 Physics-based Animation
